# Local librarian setup

This is a fictional example. Keep the completed version outside the public skill checkout.
Paths can name different directories or drives.

Workflow: `/vaults/work/+sync/Sync workflow.md`
Working vault for this startup location: `work`

| Vault name in metadata | Path on this machine |
|---|---|
| personal | `/vaults/personal` |
| work | `/vaults/work` |

Sync template: `/vaults/work/Templates/_sync_fields_javascript.md`
Type guidance: `/vaults/work/+sync/Types.md`

Use configured Obsidian connections for note discovery and graph context.
Use filesystem operations for edits, copying, and whole-file hashes.
Record any actual access limits here.

## Startup pointer

Adapt this text for the startup file that your agent reads:

```text
# Example startup instructions, not a command.
For cross-vault work, read /vaults/librarian.local.md and its current workflow.
Use the librarian skill at /tools/librarian-skill/SKILL.md.
The user works in the work vault unless they say otherwise.
```
